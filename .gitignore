# --- OS noise ---
.DS_Store
Thumbs.db
Desktop.ini

# --- Editor / IDE ---
.idea/
.vscode/
*.swp
*~

# Scratch / work-in-progress files
*.local
_scratch/
WIP_*

# Build output and Go artifacts (subprojects keep their own .gitignore too)
/build
/tmp

# Guest artifacts embedded at build time (just build fetches/builds them)
/pkg/guestartifacts/kernel-arm64
/pkg/guestartifacts/initfs.cpio

# Engineering journals (docs/): local notes and measurement records, kept
# next to the code and out of the repository — the README is what ships.
/docs/
